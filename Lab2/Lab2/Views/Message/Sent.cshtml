@model IEnumerable<Lab2.Models.ViewModels.SentMessageViewModel>

@{
    ViewBag.Title = "Sent";
}

<h2>Sent</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subject)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Receivers)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", "MessageController", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete","MessageController", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
